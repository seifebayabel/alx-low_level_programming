#include "main.h"

/**
 * _isalpha - cheks for alphabets
 * @c: charactor to be checked
 *
 * Return: 1 if charactor is a letter, 0 otherwis
 */
int _isalpha(int c)
{
	for ((c >= 'a' && c <= 'z') || (c >= 'A' && (c <= 'Z'))
			return (1);
			else
			return (0);
			}
